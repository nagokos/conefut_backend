// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/nagokos/connefut_backend/ent/applicant"
	"github.com/nagokos/connefut_backend/ent/recruitment"
	"github.com/nagokos/connefut_backend/ent/user"
)

// Applicant is the model entity for the Applicant schema.
type Applicant struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// ManagementStatus holds the value of the "management_status" field.
	ManagementStatus applicant.ManagementStatus `json:"management_status,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// RecruitmentID holds the value of the "recruitment_id" field.
	RecruitmentID string `json:"recruitment_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ApplicantQuery when eager-loading is set.
	Edges ApplicantEdges `json:"edges"`
}

// ApplicantEdges holds the relations/edges for other nodes in the graph.
type ApplicantEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Recruitment holds the value of the recruitment edge.
	Recruitment *Recruitment `json:"recruitment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ApplicantEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// The edge user was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// RecruitmentOrErr returns the Recruitment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ApplicantEdges) RecruitmentOrErr() (*Recruitment, error) {
	if e.loadedTypes[1] {
		if e.Recruitment == nil {
			// The edge recruitment was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: recruitment.Label}
		}
		return e.Recruitment, nil
	}
	return nil, &NotLoadedError{edge: "recruitment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Applicant) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case applicant.FieldID, applicant.FieldManagementStatus, applicant.FieldUserID, applicant.FieldRecruitmentID:
			values[i] = new(sql.NullString)
		case applicant.FieldCreatedAt, applicant.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Applicant", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Applicant fields.
func (a *Applicant) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case applicant.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				a.ID = value.String
			}
		case applicant.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				a.CreatedAt = value.Time
			}
		case applicant.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				a.UpdatedAt = value.Time
			}
		case applicant.FieldManagementStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field management_status", values[i])
			} else if value.Valid {
				a.ManagementStatus = applicant.ManagementStatus(value.String)
			}
		case applicant.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				a.UserID = value.String
			}
		case applicant.FieldRecruitmentID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field recruitment_id", values[i])
			} else if value.Valid {
				a.RecruitmentID = value.String
			}
		}
	}
	return nil
}

// QueryUser queries the "user" edge of the Applicant entity.
func (a *Applicant) QueryUser() *UserQuery {
	return (&ApplicantClient{config: a.config}).QueryUser(a)
}

// QueryRecruitment queries the "recruitment" edge of the Applicant entity.
func (a *Applicant) QueryRecruitment() *RecruitmentQuery {
	return (&ApplicantClient{config: a.config}).QueryRecruitment(a)
}

// Update returns a builder for updating this Applicant.
// Note that you need to call Applicant.Unwrap() before calling this method if this Applicant
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Applicant) Update() *ApplicantUpdateOne {
	return (&ApplicantClient{config: a.config}).UpdateOne(a)
}

// Unwrap unwraps the Applicant entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Applicant) Unwrap() *Applicant {
	tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Applicant is not a transactional entity")
	}
	a.config.driver = tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Applicant) String() string {
	var builder strings.Builder
	builder.WriteString("Applicant(")
	builder.WriteString(fmt.Sprintf("id=%v", a.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(a.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(a.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", management_status=")
	builder.WriteString(fmt.Sprintf("%v", a.ManagementStatus))
	builder.WriteString(", user_id=")
	builder.WriteString(a.UserID)
	builder.WriteString(", recruitment_id=")
	builder.WriteString(a.RecruitmentID)
	builder.WriteByte(')')
	return builder.String()
}

// Applicants is a parsable slice of Applicant.
type Applicants []*Applicant

func (a Applicants) config(cfg config) {
	for _i := range a {
		a[_i].config = cfg
	}
}
